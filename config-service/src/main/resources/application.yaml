server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: main
          uri: https://github.com/bc26Grupo07/spring-cloud-config
          search-paths: config-data
  application:
    name: config-service
